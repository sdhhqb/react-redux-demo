var product = function (state = [], action) {
  console.log("reducer 002");
  switch (action.type) {
    case 'PRODUCT_LIST':
      return state
    default:
      return state
  }
};

export default product;